file_path = "input.txt"
with open(file_path, 'r') as f:
    input_text = f.read()

floor = 0
for char in input_text:
    if char == '(':
        floor += 1
    elif char == ')':
        floor -= 1

print(floor)
